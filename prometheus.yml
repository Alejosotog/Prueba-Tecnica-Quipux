global:
  scrape_interval: 15s

scrape_configs:

  - job_name: "quipux-api"
    metrics_path: "/prometheus"
    static_configs:
      - targets: ["api:8000"]

  - job_name: "quipux-ingestion"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["ingestion:8001"]
